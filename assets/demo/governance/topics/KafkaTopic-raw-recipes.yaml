---
apiVersion: platform.confluent.io/v1beta1
kind: KafkaTopic
metadata:
  name: raw-recipes
  namespace: ${NAMESPACE}
spec:
  name: raw.recipes
  replicas: 3
  partitionCount: 1
  kafkaRestClassRef:
    name: default
    namespace: ${NAMESPACE}